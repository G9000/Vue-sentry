name: 'test'
on: # rebuild any PRs and main branch changes
  pull_request:
  push:
    branches:
      - master

jobs:
  test: # make sure the action works on a clean machine without building
    runs-on: ubuntu-latest
    steps:
      - uses: getsentry/action-release@v1
      - name: Create Sentry release
        uses: getsentry/action-release@v1
        env:
          SENTRY_AUTH_TOKEN: '35d7969ad5a7433190a4f20903e67e44afe18c23de0e467ea965decec410ffee'
          SENTRY_ORG: '/julio-test-9000'
          SENTRY_PROJECT: '/julio-test-9000'
          MOCK: true
